import 'package:fluent_ui/fluent_ui.dart';

class PythonSettings extends StatelessWidget {
  const PythonSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Expander(
      header: Text('Python'),
      content: Column(
        mainAxisSize: .min,
        crossAxisAlignment: .start,
        children: [

        ],
      ),
    );
  }
}
